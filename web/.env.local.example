# Insult AI API (FastAPI). Local dev = the uvicorn from api/ (port 8080).
# Prod = the Azure Container App URL. It's a build-time public var
# (NEXT_PUBLIC_*), baked into the static export — set it before `npm run build`.
NEXT_PUBLIC_API_URL=http://localhost:8080
